<!-- Configuration file used by Echoes of Eolis and FaxIScripts (https://github.com/kaimitai/faxedit) -->
<!-- User overrides -->
<eoe_config>

	<!-- region definition overrides -->
	<regions>
	</regions>

	<!-- pointer overrides -->
	<pointers>
	</pointers>

	<!-- constant overrides -->
	<consts>
	</consts>

	<!-- value set overrides -->
	<sets>
	</sets>

	<!-- boolean overrides -->
	<bools>
	</bools>

	<!-- byte map overrides -->
	<byte_to_string_maps>
	</byte_to_string_maps>

</eoe_config>
